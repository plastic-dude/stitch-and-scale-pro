# Cycle 62 desktop tab-strip notes

At 1280px, the workspace tab strip is horizontally scrollable with `scrollWidth≈4678px` inside a `clientWidth≈1214px` container. Its class includes `justify-center`. At initial `scrollLeft=0`, the visible strip begins with the tail of `Submissions` and then `Lookbook`, `Spec Sheet`, `Distribution`, `Listing SEO`, `Ad Break-Even`, `Sample & Launch`, `Collab Deal Math`, `Photo ROI`, `Video & Social`, and `Show ROI`. Core tabs Sections, Preview, and Yarn have bounding boxes far to the left of the viewport and are not visible at the initial position. At the end position, Payback Lab becomes visible, confirming that the row is centered/translated rather than naturally starting at the first tab.

The screenshot `desktop-tab-strip--start__desktop-1280.png` visually confirms that a desktop user entering the workspace cannot see the core Sections/Preview tabs without manually recovering the horizontal scroll position. This is separate from the mobile All Labs issue and should be reported to REVIEWER as a discoverability defect.

Latest main source anchor at head 3ab557a: `artifacts/stitch-and-scale/src/pages/project-workspace.tsx` lines 1032–1033 mount the desktop `<TabsList>` with `hidden lg:flex lg:flex-nowrap ... overflow-x-auto`; its computed live class also includes `justify-center` from the TabsList primitive. CHK-125 lines 992–998 correctly hide the mobile group-chip row at desktop, but the centered overflow remains: the 79-tab strip has scrollWidth≈4678px and clientWidth≈1214px, while Sections/Preview/Yarn are all offscreen at initial scrollLeft=0.
