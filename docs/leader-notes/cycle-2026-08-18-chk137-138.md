# Cycle log — CHK-137 / CHK-138 (2026-08-18)

## Item

User-flagged defect after CHK-136: English fragments visible on the German workspace —
the header gauge byline (`Maschenprobe: 20sts × 28rows / 4in`) and the grading table unit
suffixes (`sts` / `rows`). This is the same defect class CHK-130/CHK-131 retired on
controls, now applied to the workspace copy surface. CHK-138 is the follow-up: verify the
fix across all five locales and enforce documentation discipline (all evidence inside the
repo under `docs/`, never sandbox-side files).

## Reproduction

`artifacts/stitch-and-scale/src/pages/project-workspace.tsx` hardcoded the suffix strings
`sts` / `rows` directly into the header byline and the grading table cells, and
`workspace-copy.ts` (the shared localization seam already used by all other workspace
copy) had no entry for them. The fix therefore lives entirely in the copy module.

## Fix (commit `ca10aad`)

`workspace-copy.ts` gained `workspaceGaugeByline(language, gauge)` plus exported
`STS_UNIT` / `ROWS_UNIT` maps covering EN/DE/FR/ES/PT (DE/FR use `M`, FR rows `rg`,
ES `p`/`h`, PT `p`/`c`, EN unchanged). `project-workspace.tsx` now renders the byline
and grading cells through the module with the active settings language. Values, unit
logic, and the English fallback for unknown language codes are untouched. Regression:
new `src/lib/workspace-copy-gauge.test.ts`, 8 tests (all five locale bylines, `cm` unit,
missing-value dashes, null-gauge and unknown-code fallbacks, suffix completeness).

## Gates

`tsc --noEmit` clean; vitest 1,900 / 1,900 (130 files) at rebased HEAD; production build
5.67s (pre-existing large-chunk warning, unchanged). Cold-load visual on the German
workspace showed `Von Local QA • Maschenprobe: 20M × 28R / 4in` with no runtime errors.

## CHK-138 verification (all five locales)

Measured directly from the copy module source (single source of truth, `tsx` evaluation,
no product code touched):

| Locale | Gauge byline rendered |
|---|---|
| en | `20sts × 28rows / 4in` |
| de | `20M × 28R / 4in` |
| fr | `20M × 28rg / 4in` |
| es | `20p × 28h / 4in` |
| pt | `20p × 28c / 4in` |
| de (cm) | `20M × 28R / 4cm` |

## Honest limits (for Reviewer/QA)

1. The `h1` "Localization Audit" is the project's stored name (created by the local QA
   agent) — user data, correctly preserved across locales and not translated.
2. Remaining bounded language gaps, each a separate future fix: the Sections empty-state
   body (`Divide your pattern into logical sections...`) and `Add First Section` button;
   the Gift Card checkbox label `Expiry dates & dormancy fees legal in your state`; the
   Gift Card stat-margin suffix `Spanne …% of face value`.
3. The remote had advanced with Reviewer commit `641ccd8` (S272 fee-truth ledger, S275
   touch-target residue sweep, S276 contrast token, S277 tab-strip scroll cue, S241
   canonical label) between pull and push; CHK-137 was rebased on top and the suite
   re-verified green at the rebased HEAD.

## Documentation discipline (standing order from user)

All cycle evidence, verification tables, and honest-limit notes are recorded inside the
repo under `docs/` (this file). Sandbox-side evidence files are not shipped to the user
as deliverables.

## Backlog state

GitHub: 0 open issues, 0 open pull requests (measured via the authenticated API at push
time). Next CHK items are the three bounded gaps above, or a new QA cycle / inbound issue.
