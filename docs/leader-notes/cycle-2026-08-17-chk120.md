# Cycle log — CHK-120 (2026-08-17)

## Item
QA cycle 59 filed three issues: #61 CRITICAL (landing demo CTA 404, fixed by CHK-119), #62 MAJOR (79-tab wall on mobile/tablet), #63 MINOR (first-paint stability). CHK-120 worked the highest remaining severity: **#62**.

## Fix (commit `d6c4a51`)
Added a responsive grouped Labs navigator (`src/components/tab-navigator.tsx`) with localized copy (`src/lib/tab-navigator-copy.ts`, 5 locales). Mobile/tablet (<1024px) get a bottom sheet listing all labs grouped by the six workspace groups; the desktop gets a ListTree dropdown menu with grouped submenus. The existing flat strip stays as the desktop accessibility surface. Both surfaces read from the same `tabGroupsFromRegistry()` source, so they can never drift apart. No calculation semantics changed. 9 registry-parity regression tests added (all 80 tab values exactly once, per-group counts vs TAB_GROUPS, group order, 5-locale copy shape).

## Gates
- `tsc --noEmit`: clean
- vitest full suite: 1818/1818 (116 files)
- build: green
- localhost visual (German UI): dropdown opens with all 6 groups; channels submenu renders localized lab names (Trunk Show, Kanäle, ...); zero runtime errors.

## Honest notes (for Reviewer/QA)
1. **Group header strings render in English** ("Design & Pattern", "Sizing & Fit"...) inside the dropdown/menu because `TAB_GROUP_LABELS` is a hardcoded English map. Lab names are localized. Making group headers locale-aware is a separate small CHK if desired.
2. The mobile (<1024px) sheet could not be physically driven in the sandbox browser (viewport cannot be resized below desktop width); the sheet markup is verified structurally and covered by the parity tests. A real-device check is welcome.
3. Dropdown shows 79 visible leaf items plus 6 group submenu triggers = all 80 entries reachable.

## Issue status
- #61: closed (CHK-119, evidence commented)
- #62: closed (CHK-120, evidence commented)
- #63 MINOR: remains open — next highest severity
- #13: open (window.print human spot-check, sandbox-unworkable)
