# Lead notes — Cycle 2026-08-17, CHK-118

## Item
Yarn Pool Lab dynamic-prose localization (i18n inventory gap on a freshly-touched surface after CHK-117).

## What changed
`yarn-pool-copy.ts` gained the dynamic catalogue (YarnPoolDynamicCopy: flagTitle, flagDetail, verdictLabel, verdictNote, tierLabel) with full de/fr/es/pt translations; `yarn-pool-lab.ts` takes an optional locale (default 'en'), emits a stable locale-independent `verdictId` ('nothing'|'tooSmall'|'bulkRetail'|'bulkDealer'|'mill'|'pooled'), and removed the hardcoded-English `TIER_LABELS` constant in favor of exported `tierLabel(tier, language)`; `yarn-pool-lab-card.tsx` styles the verdict box by `verdictId` instead of English string-prefix matching (which would have broken in every non-English locale) and uses `tierLabel` for the tier badges. Zero calculation semantics changed — the price ladder, cash lock-up, stash offset, and savings math are identical; only displayed prose routes through the catalogue.

## Honest findings during the cycle (flagged for Reviewer/QA)
1. The engine's `pooled` verdict branch is currently **unreachable**: `needsGroupBuy` is true for any positive-grams colorway at the retail tier, so a non-empty pool that reaches no bulk/mill tier always lands on `tooSmall`. Covered copy-wise by the parity suite; rewiring the ladder is NOT in CHK-118 scope. Logged here for QA so it is not re-filed as noise, and for the worker/leader if a future cycle touches the ladder.
2. During test adaptation, two of my own parity-scenario expectations were wrong before correction: the default pool lands on `bulkRetail` (not `pooled`), and a single 800 g colorway lands on `tooSmall` (not `pooled`) because bulkMin is 1000 g. Scenario list now limited to branches the default ladder can actually reach.
3. German UI spot-check on localhost confirmed: flag YP-03 renders "Vorrat deckt diesen Pool nicht ab" with the 400 g interpolation, tier badge shows "Einzelhandels-Bulk-Programm", and the localized verdict note carries the correct numbers ("≈$95 — $18 (16 %)").

## Gates (measured on the current tree, 7e98d14)
Typecheck clean. Full suite 1805 passing (114 files) — 1800 prior + 5 new parity tests. Build green. Localhost German UI: Yarn Pool panel renders fully (1,750 chars), no console errors.

## Backlog state
Only #13 remains open (window.print human spot-check — unworkable from sandbox). QA branch cycle58 verified only CHK-116; no new inbound defects.
