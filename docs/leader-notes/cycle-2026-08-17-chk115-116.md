# Cycle 2026-08-17 — CHK-115 & CHK-116 + full inbox close-out

## CHK-115 (ce1f185) — QA #42
Listing Test Lab queue previously mislabeled total expected value with an /hr suffix and ranked by raw total EV. The engine now computes a true `evPerHour` per listing, sorts the queue by it, and the card displays EV per re-list hour. The same cycle corrected two misleading Yarn Pool captions (`tierNote` phantom "advanced fields"; `memberNote` overstatement) across all five locales via the copy catalogue. Three regression tests lock the EV/hr semantics.

## CHK-116 (f9799fa) — QA #60 + S260
Consignment Re-Price Lab: fixed the controlled→uncontrolled input flip on the zero-sold scenario. Stored state now hydrates over canonical defaults (stale blobs missing a key can never reach an input as undefined) and setState silently drops undefined patch entries. Card, defaults, and reset now go through shared exported helpers (`REPRICE_DEFAULTS`, `hydrateRepriceState`, `applyRepricePatch`). Also localized the hardcoded English zero-sell-through ladder footer into all five locales (`zeroStockFooter` in `consignment-reprice-copy.ts`). Three regression tests (32→35). Evidence screenshot: `docs/screenshots/chk116-reprice-lab-zero-sold-no-flip.webp`.

## Inbox sweep result — backlog effectively empty
All open issues resolved or closed with evidence in this firing:

| Issue | Resolution |
| --- | --- |
| #60 | Fixed by CHK-116, evidence comment, closed |
| #42 | Fixed by CHK-115, evidence comment, closed |
| #41 | Verified fixed-in-flight (intro, Verdict label, units), closed |
| #40 | Fixed by CHK-114, evidence comment, closed |
| #47 | Verified fixed-in-flight (Podcast tab mount + Magazine ×100), closed |
| #16 | Not a defect on current tree (crash class retired), closed |
| #15 | Not a defect — two-row wrap is the deliberate design, closed |
| #14 | Verified fixed-in-flight (signed money), closed |
| #12 | Verified correct per CYC bands, closed |
| #11 | Verified correct per CYC reference, closed |
| #13 | LEFT OPEN — `window.print` paths need a human browser spot-check; triage comment posted |

## Next firing
Inbox is empty pending #13's human spot-check. Per the playbook, the next CHK should come from an inbound issue, a Reviewer sweep, or the remaining long-open S-ledger MAJORs already verified fixed in code (S182, S251, S123, S160). No competitor/market research is scheduled — backlog-only mandate stands.
