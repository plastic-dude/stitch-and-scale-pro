# Work Queue — continued queue protocol

**Canonical file for the 15-minute loop firings.** This file is the single source of truth for "what is next." Every firing reads it first, works the first `queued` entry, updates statuses inline, and appends a ledger line. Owner directive 2026-08-19: firings continue this queue from the start, never restart from scratch.

## Queue entries (walking order)

| # | id | Description | Severity | Status | Evidence / notes |
|---|---|---|---|---|---|
| 1 | RESTUDY-001 | Full chronological restudy S001→HEAD | QUEUE-ITEM | done | `97897be`; details in `docs/leader-notes/restudy-2026-08-19-S001-to-HEAD.md` |
| 2 | LEDGER-HYGIENE-001 | Strike stale escalations (S182/S251 fixed in flight, S160 REQUIRES-REPRODUCTION, bundle partners functional); add Reviewer rule against re-escalating fixed items | QUEUE-ITEM | done | `15b473b`; `docs/team-standing-orders.md` updated |
| 3 | QUEUE-001 | QA coverage gap: localization sprint (CHK-137..139, localizations surfaces) had no third-party audit since QA stopped Aug 17 22:17 | MAJOR | done | Run id `2026-08-19-Q001`; `33611c1`. Audit report: `docs/leader-notes/cycle-2026-08-19-queue001-audit.md` — 12 findings: all CHK-139 fixes hold under fresh DE reload; defects confirmed: 0-measurements chip (closed as CHK-141 next firing), delete toast (toast module, next part of QUEUE-002), grading sheet page (4 strings, QUEUE-009), tester-desk + transbundle Tier 1 narratives (QUEUE-003/004 scope), HMR crash class (3 components, dev-only, QUEUE-010); mobile viewport eyes-check unavailable in this sandbox (limitation, not defect) |
| 4 | QUEUE-002 | Localization pass 1 of 2 — free small items: `0 measurements` chip (`workspace-copy.ts` `measurementsChip` helper, 5 locales) and toast/snackbar module (`toast-copy.ts`) | MINOR | done | Part 1/2 DONE as CHK-141 (`measurementsChip` + wire + 4 regression tests; live `0 Maße` under DE; `docs/leader-notes/cycle-2026-08-19-chk141-queue002-chip.md`). Part 2/2 DONE as CHK-142 (`toast-copy.ts` module + ~30 sites wired across pages/cards; toast regression tests; live-verified delete toast `Abschnitt gelöscht` under DE; evidence in `docs/evidence/q002-toast-loeschung-de.webp` + `docs/leader-notes/cycle-2026-08-19-chk142-queue002-toast.md`). Note: final toast call-count was ~78, not the audit's 93 — card-internal copy objects (`copy`/`copyText` with their own toast keys) cover most; `toast-copy.ts` covers only the previously bare literal strings (per-module scope note in its header) |
| 5 | QUEUE-003 | Localization pass 2 of 2 — Tier 2 narratives ("Benchmarks baked in" footers, 9 cards) + ~95 `<Field hint>` strings across 8 files | MINOR | done | CHK-143. 273 strings hand-translated de/fr/es/pt across 18 files: 11 new copy modules (`src/lib/*-copy.ts`), 7 seamed modules extended, all 18 components wired; 77 regression tests (2,065 total); live DE Musterclub check — all hints German, literal `label="{copy.soloPrice}"` bug fixed (9 sites) → `Einzelpreis ($)`; evidence `docs/evidence/q003-patternclub-hints-de.webp`; log `docs/leader-notes/cycle-2026-08-19-chk143-queue003.md`. Honest limits: Tier 1 narratives → QUEUE-004; verdict-note/FAQ planner output prose out of scope (needs owner localization decision) |
| 6 | QUEUE-004 | Localization pass 3 of 2 — Tier 1 narrative cards (translation-bundle, testknit-desk, deals-tab intro+benchmarks, kal-planner, submission-desk, teach-economics: 9 paragraphs) | MINOR | done | CHK-145 absorbed the scope: translation-bundle, testknit-desk, deals-tab, teach-economics UI surfaces fully wired to 5-locale copy modules; honest limit — lib-authored long-form prose narratives (benchmark/verdict paragraphs computed by libs) were NOT translated, needs owner decision (logged `docs/leader-notes/cycle-2026-08-21-chk145.md`) |
| 7 | QUEUE-005 | #52 workspace legend overcount — Crawler confirmation at the current build | MINOR | done | CHK-150: defect NOT reproducible at HEAD — eyes-check + CDP invariants at 360/390/430px: chip counts derive from TAB_REGISTRY (single source of truth), 79 = 79 parity with the desktop strip, 6 chips sum to total, zero duplicate entries, chip click activates the correct grouped tab, mobile dropdown lists all 79 triggers with 0 duplicates; per-group registry counts (22/15/13/12/10/7) cross-validated via vitest against the displayed chip labels; regression-gated by CHK-087/089/091 count-drift guards. Evidence `docs/evidence/chk150-w{360,390,430}-legend.png`; log `docs/leader-notes/cycle-2026-08-21-chk150.md` |
| 8 | QUEUE-006 | S160 "migration delta" — concrete reproduction or final strike | MAJOR | done | CHK-151: repro DONE — seeded every legacy shape: object path correct, but TWO silent-loss deltas found and FIXED in `storage-lib.ts:migrateFrom` (old `typeof parsed === 'object'` guard silently orphaned primitive legacy values — card read null forever with legacy key left in place; partitioned blobs defaulted primitive partitions to `{}` with legacy removed). Fix: any parseable value folds verbatim when scoped key empty; conflict/corruption rules unchanged; consumer hydrate helpers degrade gracefully to defaults (verified). New 12-test `storage-migration.test.ts`; tsc clean; vitest 2,142/2,142 (152 files); build green; log `docs/leader-notes/cycle-2026-08-21-chk151.md` |
| 9 | QUEUE-007 | GitHub live state re-verification (connector needed) | QUEUE-ITEM | blocked | Human action: GitHub connector not enabled in this environment; last-known 0 open issues / 0 open PRs at CHK-138 is UNVERIFIED as of Aug 19 |
| 10 | QUEUE-009 | Grading sheet page English strings — "Back to Project", "Copy TSV / CSV / Print Sheet", "BASE SIZE", "GAUGE" in `project-grading.tsx` (4 strings) | MINOR | done | Fixed and wired in CHK-145 (`project-grading.tsx` uses `lab-stat-copy` keys, 5 locales) |
| 11 | QUEUE-010 | HMR crash class — `useState(useMemo(...))` initializer pattern crashes under Vite 7 HMR with "Cannot read properties of null (reading 'useState')"; 3 components in one session (`giftcard-lab-card.tsx:132`, `testknit-desk-card.tsx:80`, `translation-bundle-card.tsx:113`). Dev-only; vitest/prod unaffected | MINOR | done | CHK-152: whole class retired via a seam — `storage-lib.ts` gained `useProjectStorage<T>` (memoized handle, keyed on prefix/id/legacyKeys/partition) + `useProjectStorageState<T>` (memoized derivation + value-or-fn updaters + persist-by-effect, no lazy initializer ever touches a handle). All 18 affected cards converted: giftcard, testknit-desk, translation-bundle, trunk-show, submission-pipeline, kal-roi, channel-funnel, club-revenue, wholesale-book, hire-vs-self, inclusive-sizing, pattern-club, pattern-license + sweep-found final five (membership, promotion, price-window, retention, platform-mix, each with legacy keys folded in); guard test `hmr-crash-class.test.ts` pins the class (3 signatures, 18 cards); cycle log `docs/leader-notes/cycle-2026-08-21-chk152.md` |
| 12 | QUEUE-011 | Brutal Mobile & Resilience Audit (2026-08-21) findings F-01+F-02 (CRITICAL) — impossible base values accepted/persisted and Grading Lab unable to bless corrupt data: G-09 integrity preflight in `grading-lab.ts` (blocks verdict on any non-finite/<=0 `baseValue`), save-path validation + localized destructive toast in `project-workspace.tsx` (via `toast-copy.ts`), CSV import guard in `csv-import.ts`, G-09 flagTitles + FLAG_DETAILS in `grading-copy.ts` (5 locales) | CRITICAL | done | CHK-144. `6beb4ad` (ref will be the CHK-144 commit after amend); 10 net new tests (2,076 total); live DE evidence `docs/evidence/chk144-integrity-guard-de.webp`; log `docs/leader-notes/cycle-2026-08-21-chk144.md`. Severity exception recorded: this outranked the queued QUEUE-004 (MINOR localization) because F-01/F-02 are live correctness defects threatening verdict trust |
| 13 | QUEUE-012 | Audit F-03 (MAJOR) — financial calculators accept negative/extreme inputs; add semantic validation | MAJOR | done | CHK-147: `validate-field.ts` shared layer (10 typed field rules, quarantine gate `validateInputs` + `isInputValid`/`invalidSummary`, 22 tests) + first consumer wired: Pricing Advisor (`advisePrice` returns quarantined advisory on invalid input; card validates raw strings, error panel, dash rendering, downstream consumers quarantined) — see ledger + cycle log | | 14 | QUEUE-013 | Audit F-04 (MAJOR) — PDF export stuck in "Preparing your PDF…"; add state-machine / timeout fallback | MAJOR | done | CHK-153: `print-utils.ts` rewritten as a result-reporting state machine — typed `PrintAttemptResult` on every path, module in-flight lock (double-click protection, synchronous `reason: 'blocked'`), `onafterprint` registered on BOTH print surfaces (desktop iframe contentWindow + mobile new window) releasing the lock and removing the iframe only after the dialog actually closes (old blind 5s removal race gone); page `handleExport` consumes the attempt: failure → destructive localized toast + immediate button re-enable; success → 6s bounded fallback timer + afterprint listener exits the export state deterministically (old blind 1500ms reset removed); `exportFailed` added to PdfLabels interface + all 5 locales; 16 new tests in `print-utils.test.ts` pin the contract (typed returns on every return site, lock release paths incl. both afterprint registrations, single safe writeInto, no 5000/1500 blind timers, page afterprint teardown, locale ×6) | tsc clean; vitest 2,160/2,160 (154 files, +16); build green (6.42s); cycle log `docs/leader-notes/cycle-2026-08-21-chk153.md` | a2934ad768f914d3587f4517c7826217c62d275c | QUEUE-014 (F-05 filename sanitization + F-08 Chart Lab false-positive Ready, MINOR) | 15 | QUEUE-014 | Audit F-05 (MINOR) filename sanitization + F-08 (MINOR) Chart Lab false-positive Ready | MINOR | queued | | 16 | QUEUE-015 | Audit F-06 (MINOR) deep-link semantics on fresh device + F-09 draft token policy + F-10/F-11 (MINOR) sticky footer / generic errors | MINOR | queued | | 17 | QUEUE-016 | Audit F-07 (NITPICK) dense tab navigation IA + F-12 benchmark labels + F-13 wildcard CORS + F-14 SPA 200-for-missing-paths (mostly product/arch decisions) | NITPICK | queued | Audit files: `docs/leader-notes/audit-2026-08-21-brutal-mobile-resilience.md` + `audit-2026-08-21-notes.md` |
| 18 | QUEUE-017-NEW | Extended audit E-01 (MAJOR) — shared semantic-validation layer must be built BEFORE per-module patches: typed `validateField` (type, required, finite, min/max, unit, domain: nonneg money, positive denominators, 0–100 %, nonneg counts/duration, chronological dates) consumed by all calculators | MAJOR | queued | Extended audit `docs/leader-notes/extended-audit-2026-08-21.md` — "P0 decision-integrity defects" across 25+ modules; single central gate fixes the whole class; supersedes ad-hoc per-module patches in QUEUE-012 scope order. SUPERSEDED: merged into QUEUE-012 and delivered as CHK-147 — no separate work item |
| 19 | QUEUE-017-NaN | Extended audit E-02 (CRITICAL) — `NaN`/`Infinity` leak into user-facing output (Repeat cold-acquisition `$NaN`, Wholesale & Book `Infinity` breakeven, Ad Break-Even `∞` ROAS): non-finite assertion at every calculation boundary + no stringify of non-finite into prose | CRITICAL | done | CHK-146: `numeric-guard.ts` (finiteOr / finiteNarrative helpers, 16 tests) + wholesale-book breakeven narrative quarantine, retention planner cold-acquisition guard + card fallback, ad-break-even best-channel prose suppression on invalid price; 19 net new tests; live zero-input repros under FR verified (`docs/evidence/chk146-*.webp`); see ledger + cycle log |
| 20 | QUEUE-017-COPY | Extended audit E-03 (MAJOR) — paste-ready copy not quarantined: Listing SEO kit, Collab counter-offer letter, agreement text, tier copy, receipts carry invalid values even under conservative verdicts; universal rule `copyable === valid && finite && complete` (copy/share/export disabled in invalid state) | MAJOR | queued | Repros: collab deal math (`$-1399` counter-offer), kit economics agreement (`200/-100 split`), membership tier copy, wholesale & book `MAYBE` + advice |
| 21 | QUEUE-017-GATE | Extended audit E-04 (CRITICAL) — grading readiness semantics: impossible measurement saved → Grading Lab still `Ready` (F-02 partially fixed by CHK-144 G-09 on its own path, but audit confirms verdict trust requires project-wide `gradeable !== valid` distinction); verdict must be scoped e.g. `Ready (checked rules)` and an invalid measurement must make project status `Invalid data` | CRITICAL | done | CHK-146 verified closure: G-09 preflight (`src/lib/grading-lab.ts` invalidMeasurements blocks sets with non-finite/≤0 baseValue, tested G-09 for -9, NaN, Infinity, 0) plus CHK-144 save-path validation means the audit's `-9` repro cannot be saved at the current tree — the grading-lab path is closed. CHIP GAP CLOSED as CHK-148: new `project-validity.ts` per-project analyzer (measurements/gauge/dimensions) + AutosaveIndicator `Saved — invalid data` amber chip with invalid-item tooltip, wired in shell.tsx; live FR evidence |
| 22 | QUEUE-018 | Owner request 2026-08-21 — onboarding truth audit: onboarding overlay must match current app state; never stress or overload the user, but must not mislead (fabricated "Optional Cloud Sync / Sign in later" card, cloud-implying welcome footer, stale "Five concepts — that's the whole app" vs 77+ labs, wrong sample measurement count 8 vs actual 11) | MAJOR | done | CHK-149: 4 misleading/stale claims reworded in all 5 locales (`i18n.ts`), philosophy card retitled "Nothing Uploaded" (DatabaseBackup icon), dashboard Local Storage Notice cloud-future promise removed (5 locales), hardcoded 'Coming soon' badge localized via new `workflow.onboarding.comingSoon` key; honest claims verified against code truth (9 sizes, 4 PDF templates, 11 crew-neck measurements, no cloud ever); 2,130/2,130 tests, tsc + build green; live EN+DE evidence `docs/evidence/chk149-*.webp`; log `docs/leader-notes/cycle-2026-08-21-chk149.md` |
| 23 | QUEUE-017-BOUND | Extended audit E-05 (MINOR) — error boundaries for lazy modules: blank render on module switch observed (Wholesale & Book open → blank page, recovery banner after reload); every lazy-loaded card needs Suspense/ErrorBoundary with retry; overlaps QUEUE-010 HMR class | MINOR | queued | No repro yet (not reproducible on demand); requires eyes-on reproduction by Crawler at 390/412px widths; do not fix without repro — standing-orders rule |
| 24 | QUEUE-017-TXN | Extended audit E-06 (MINOR) — persistence transparency: Save actions (Design Ledger `Record cost`, Receipt Lab `Save to ledger`) fail silently with no feedback; every save must visibly resolve to success/validation-failure/system-failure | MINOR | queued | Repros at Design Ledger (`design-ledger-card.tsx`) and Receipt Lab (`receipt-lab-card.tsx`); also Pipeline `Add call` creating an unsaved draft before required fields complete |

## Run ledger

| Run (date) | Item worked | Gates | Commit | Next item |
|---|---|---|---|---|
| 2026-08-21 (CHK-149) | QUEUE-018 (MAJOR) — onboarding truth audit: "Optional Cloud Sync / Sign in later" card removed (replaced by "Nothing Uploaded", DatabaseBackup icon, cards now built from i18n keys); welcome footer and dashboard Local Storage Notice cloud claims removed in all 5 locales; "Five concepts — that's the whole app" reworded to "Five core concepts — plus a lab for every way your pattern earns"; sample measurement count corrected 8→11 ("…plus a beanie"); hardcoded "Coming soon" badge localized (`workflow.onboarding.comingSoon`); install banner + non-onboarding surfaces swept — only the dashboard notice matched the cloud-claim class; truth claims verified against code (9 sizes, 4 PDF templates, 11 crew-neck measurements, restart key) | tsc clean; vitest 2,130/2,130 (151 files); build green (6.57s); live EN+DE onboarding evidence `docs/evidence/chk149-*.webp`; cycle log `docs/leader-notes/cycle-2026-08-21-chk149.md` | 6eed6c1daffdd13dd5e9fd90ed4094994983ea3b (ref lags one
 amend by construction) | QUEUE-005 (#52 legend overcount eyes-check) |
| 2026-08-21 (CHK-150) | QUEUE-005 (MINOR, closed as not reproducible) — #52 workspace legend overcount: eyes-check + CDP invariants at 360/390/430px confirm HEAD is clean: mobile chips derive counts from TAB_REGISTRY (single source), chip sum equals registered tab count (79 = 79), desktop strip parity (79 = 79), zero duplicate entries, chip click activates the matching grouped tab, mobile "All labs" dropdown lists all 79 triggers with 0 duplicates; per-group registry counts (business 22, pricing 15, launch 13, design 12, channels 10, fit 7) match the displayed chip labels exactly; the class is regression-gated by CHK-087/089/091 count-drift tests | tsc clean; vitest 2,130/2,130 (151 files); build green (6.35s); evidence `docs/evidence/chk150-w{360,390,430}-legend.png`; cycle log `docs/leader-notes/cycle-2026-08-21-chk150.md` | a18641f14dd10219381630b1bcdb69167c3037be (self-verified at commit time; CHK-151 ledger will lag one amend) | QUEUE-006 (S160 migration repro) |
| 2026-08-21 (CHK-153) | QUEUE-013 (MAJOR, closed) — PDF export stuck in "Preparing your PDF…": `window.print()` blocks the JS thread until the OS print dialog closes, so the old blind 1500ms reset could never fire while the dialog was open (and never fired at all when the print window failed to open, leaving no retry path and no feedback); fixed by converting the print surface into a result-reporting state machine (`openPrintWindow` returns typed `PrintAttemptResult` on every path; module in-flight lock against rapid double clicks; `onafterprint` on both print surfaces releases the lock and removes the iframe only after dialog close — replacing the blind 5s removal race); the page consumes the result (failure → localized destructive toast + immediate re-enable; success → bounded 6s fallback timer + afterprint-driven exit; old blind reset removed); `exportFailed` localized in all 5 locales via the PdfLabels interface; 16 new structural/runtime tests pin the contract | tsc clean; vitest 2,160/2,160 (154 files, +16); build green (6.42s); cycle log `docs/leader-notes/cycle-2026-08-21-chk153.md` | a2934ad768f914d3587f4517c7826217c62d275c (ref lags one amend by construction) | QUEUE-014 (F-05 filename sanitization + F-08 Chart Lab false-positive Ready, MINOR) |
| 2026-08-21 (CHK-151) | QUEUE-006 (MAJOR) — S160 migration repro: deliberate seeding of every legacy shape found TWO silent-loss deltas in `projectStorage().migrateFrom` — primitive legacy values (rate/count/flag tabs) were silently orphaned by the `typeof parsed === 'object'` guard (scoped key stayed empty, legacy key never removed, card read null forever), and partitioned blobs defaulted primitive partitions to `{}` with the legacy key removed; fix folds any parseable value verbatim when scoped key empty, keeps conflict/corruption rules unchanged; consumer hydrate helpers verified to degrade to defaults on non-object state; 12 new tests (`storage-migration.test.ts`) pinning all seven legacy shapes | tsc clean; vitest 2,142/2,142 (152 files, +12 net); build green (6.41s); cycle log `docs/leader-notes/cycle-2026-08-21-chk151.md` | d61cdc8d21f948ed696702332259c88a24bd5a3a (self-verified at commit time; CHK-152 ledger will lag one amend) | QUEUE-010 (HMR crash class, full 18-card sweep) |
| 2026-08-21 (CHK-152) | QUEUE-010 (MINOR, closed — full-class retirement) — HMR crash class: any card memoizing a raw `projectStorage(...)` handle and initializing persisted state with `useState(() => loadStored(handle))` crashes under Vite HMR in strict-mode double-invoke; seam added to `storage-lib.ts`: `useProjectStorage<T>` (memoized handle, one instance per prefix+project+legacyKeys+partition, legacy key folded by the handle's own migration) + `useProjectStorageState<T>` (memoized derivation from the live handle, value-or-fn updaters, persist via `useEffect([stored, scopedKey])` — no lazy initializer touches a handle). First pass converted 13 cards; a deliberate full-component sweep found 5 more (membership/promotion/price-window/retention/platform-mix, legacy keys `mspl-v1`, `promo-v1`, `prcw-v1`, `pmix-v1`), converted and brace-verified; guard test `hmr-crash-class.test.ts` pins all 18 cards against 3 forbidden signatures + loadStored purity + seam exports; pre-seam cards test-knit/tech-edit inspected (event-handler write paths, not in class) | tsc clean; vitest 2,144/2,144 (153 files, +2 guard); build green (6.42s); cycle log `docs/leader-notes/cycle-2026-08-21-chk152.md` | 79aeac440382cb08e42560d1e1ddf65ec25e56fc | QUEUE-013 (F-04 PDF export stuck, MAJOR) |
| 2026-08-19 (restudy+hygiene, pre-queue session) | RESTUDY-001 + LEDGER-HYGIENE-001 | docs only | `15b473b` | QUEUE-001 |
| 2026-08-19-Q001 | QUEUE-001 audit (12 findings) + QUEUE-009/010 registered; chip fix DEFERRED per one-fix-per-cycle | docs; gates green at `43978d8` (tsc clean, 1,978/1,978 tests, build green), re-verified at `9a749fe` | `9a749fe` | QUEUE-002 (chip first, toast second) |
| 2026-08-19-Q002 | QUEUE-002 part 1/2 — measurementsChip (5 locales, singular/plural, 0-count plural) + wire + 4 regression tests + live DE browser check (`0 Maße` chip, German delete dialog, sandbox storage restored) | tsc clean; vitest 1,981/1,981 (135 files); build green; fresh browser check pass | `f0d4078` (refs lag one amend by construction) | QUEUE-002 part 2 (toast/snackbar module) |
| 2026-08-19-Q003 | QUEUE-002 part 2/2 — toast/snackbar module: `toast-copy.ts` (ToastCopy interface, 5 locale maps, interpolation helpers) + wiring across settings/project-workspace/project-pdf/project-grading/dashboard/storage-health/publish-toolkit/show-roi/teach-economics/tech-edit/test-knit/wholesale-book/yarn-buy/yarn-pool/translation-bundle/partner-economics + 7 toast-copy regression tests + live DE delete-toast check (`Abschnitt gelöscht`) | tsc clean; vitest 1,988/1,988 (136 files); build green; fresh browser check pass | `9869f5e` (refs lag one amend by construction) | QUEUE-003 (Tier 2 narratives + field hints) |
| 2026-08-19-Q004 | QUEUE-003 — Tier 2 narratives + field hints: 273 strings hand-translated de/fr/es/pt across 18 files; 11 new typed copy modules, 7 seamed modules extended, all 18 components wired (incl. fixes for literal `{copy.xxx}` label bug in pattern-club, helper-scope copyText in show-roi/wholesale/testknit-slot); 77 regression tests; live DE Musterclub evidence (`Marktspanne`, `Gründungskohorte`, `Einzelpreis ($)` etc.) | tsc clean; vitest 2,065/2,065 (147 files); build green; fresh DE browser check pass | `5c6236b` (ref lags one amend by construction) | QUEUE-004 (Tier 1 narratives) |
| 2026-08-21 (CHK-144) | QUEUE-011 (audit F-01+F-02, CRITICAL) — integrity guard: save-path baseValue validation + localized toast; CSV import guard; G-09 grading-lab preflight + 5-locale copy; 10 net new tests (2,076 total); live DE toast evidence (`docs/evidence/chk144-integrity-guard-de.webp`); audit files archived in `docs/leader-notes/`; QUEUE-012..016 registered | tsc clean; vitest 2,076/2,076 (148 files); build green; live DE check pass | `6beb4ad` (ref lags one amend by construction) | QUEUE-004 (Tier 1 narratives) |
| 2026-08-21 (CHK-145) | Owner language-defect directive: full app language sweep — 123 bare-English findings grouped across 74 lab cards + page surfaces; new shared `lab-stat-copy.ts` (~260 keys, 5 locales) + 4 new copy modules; 29 components/pages wired via `useSettings()`; QUEUE-004 and QUEUE-009 absorbed; typecheck/vitest/build gates green; live DE + FR evidence (`docs/evidence/chk145-samplelaunch-de.webp`, `chk145-sample-launch-fr-locales.webp`); cycle log `docs/leader-notes/cycle-2026-08-21-chk145.md`; honest limits: lib-authored long-form prose narratives untranslated (owner decision needed); project-name heading is user-owned metadata | tsc clean; vitest 2,076/2,076 (148 files); build green; live DE + FR checks pass | `a21b6b2` (ref lags one amend by construction) | QUEUE-005 (#52 legend overcount) |
| 2026-08-21 (CHK-147) | Owner directive "working on everything, not queuing" — continuous backlog firing. QUEUE-012 + QUEUE-017-NEW (E-01 shared validation layer): `validate-field.ts` (money/rate/percent/ratio/count/duration/positive-denom/positive/date-iso/string; NaN/Infinity/emptiness/bounds; `validateInputs`/`isInputValid`/`invalidSummary`, 22 tests); Pricing Advisor quarantined (`advisePrice` validates first, quarantined advisory with `recommendedPrice: NaN` + `Validation —` reason; card validates raw strings before parsing, error panel, `—` dash rendering, empty-quarantine table header fixed); quarantine reaches launch-campaign/listing-seo/release-portfolio free; `numeric-guard` safeNumber layer used in-card | tsc clean; vitest 2,122/2,122; build green (6.56s); live FR evidence `docs/evidence/chk147-pricing-validation-fr.webp` | 86fb367 (ref lags one amend by construction) | QUEUE-017-GATE (CRITICAL status-chip gap) |
| 2026-08-21 (CHK-148) | QUEUE-017-GATE (CRITICAL) — project-wide data-validity flag: new `project-validity.ts` analyzer (section/measurement bounds, zero base values, gauge completeness, pattern-dimension sanity, cap of 3 reported items), `invalidMeasurements` exported from grading-lab, AutosaveIndicator invalid state (amber chip "Saved — invalid data" + tooltip listing invalid items and fixes, 5-locale copy via lab-stat-copy), shell.tsx wiring via useProjects; 8 regression tests (`project-validity.test.ts`) | tsc clean; vitest 2,130/2,130 (149 files); build green (6.6s); live FR evidence `docs/evidence/chk148-invalid-data-chip-fr.webp`; demo project restored to valid state after repro | cbd9e22 (ref lags one amend by construction) | QUEUE-005 (#52 legend overcount eyes-check) |
| 2026-08-21 (CHK-146) | Audit reconciliation + QUEUE-017-NaN (E-02, CRITICAL, severity exception over queued QUEUE-005 MINOR) — `numeric-guard.ts` boundary helpers (finiteOr/finiteNarrative, 16 tests); wholesale-book Infinity breakeven narrative quarantined (stat + narrative + checklist); retention planner cold-acquisition NaN/Infinity guard + card fallback note; ad-break-even best-paid-channel prose suppressed on zero-net price; E-04 verified closed by CHK-144 G-09 + save-path validation (audit's -9 repro cannot save at current tree); both uploaded audits archived: `docs/leader-notes/audit-2026-08-21-brutal-mobile-resilience.md`, `audit-2026-08-21-notes.md`, `extended-audit-2026-08-21.md`; QUEUE-017-NaN done, QUEUE-012 sharpened | tsc clean; vitest 2,095/2,095 (149 files); build green; live FR repros: zero retail (wsbook), zero price (repeat + adlab), evidence `docs/evidence/chk146-*.webp` | 2743a76 (ref lags one amend by construction) | QUEUE-012 (F-03 validateField layer; next severity-exception candidate already CRITICAL in queue: QUEUE-017-GATE remains open on the status-chip gap) |
