
## Browser-confirmed deployment evidence

The public production URL again rendered a dark blank viewport with no detected interactive elements. The Vercel official REST documentation confirms that an authenticated deployment can be created or an existing deployment redeployed, that a production target assigns aliases, and that deployment state progresses through queued/building to ready or error. The production repair must therefore identify the Vercel project and deployment/alias state, trigger a deployment from the verified commit, and poll the resulting public alias rather than merely assuming that a Git push rebuilt production.

Vercel's Git documentation states that a production deployment should be created when the configured production branch receives a new commit, and that the production branch is controlled in Project Settings → Environments → Production → Branch Tracking. Vercel's Environments documentation distinguishes Preview from Production and supports manual promotion/rollback. The inspected project is Git-connected to `plastic-dude/stitch-and-scale-pro`, but recent deployments for the target project are failing before alias assignment with `lint_or_type_error` in the root `api/mcp.ts` handler.

## Fresh Vercel deployment evidence (2026-08-21)

- Official Vercel deployment API: `POST https://api.vercel.com/v13/deployments`; Git deployments can specify `project`, `target: production`, and `gitSource`, with response fields for `readyState`, `aliasAssigned`, `alias`, `errorCode`, and `errorMessage`. Source: https://vercel.com/docs/rest-api/deployments/create-a-new-deployment
- Official runtime log API: `GET /v1/projects/{projectId}/deployments/{deploymentId}/runtime-logs`; it returns a stream of runtime invocation records and requires runtime-log permissions. Source: https://vercel.com/docs/rest-api/logs/get-logs-for-a-deployment
- Commit `bb003d4` deployed to production but failed Vercel lint/type checking because `mcp-server.ts` used extensionless ESM imports.
- Commit `a5b5047` deployed but failed module resolution because `pdf/labels.ts` used the app-only `@/lib/i18n` alias in the serverless dependency graph.
- Commit `72c606c` produced Vercel deployment `dpl_2f6RtzHzuc6m5GhYXiukSP8XC59A`, `READY`, `target: production`, `aliasAssigned: true`, and aliases including `stitch-and-scale-pro-api-server.vercel.app`. The public HTML now references `assets/index-Cf9xo7xy.js` and no longer references stale `index-K0lrZ2kE.js`.
- Despite the READY deployment, `GET`, unauthenticated `POST`, and `OPTIONS` requests to `/api/mcp` returned HTTP 500 `FUNCTION_INVOCATION_FAILED`. Runtime-log stream retrieval did not return within the bounded request window. Further investigation is required; the frontend deployment is live, but the MCP serverless function is not yet publication-ready.

## CODER II firing — 2026-08-21

Fresh Vercel evidence:

- The official `FUNCTION_INVOCATION_FAILED` guidance says the error can come from the function or its environment, and requires checking application logs, reviewing function code, checking unhandled exceptions, and verifying function configuration: https://vercel.com/docs/errors/function_invocation_failed
- The official Node.js runtime guidance states that `/api` TypeScript functions may use Web Standard `fetch` exports or named HTTP method exports, that root `tsconfig.json` path mappings and project references are not supported by the Node.js runtime, and that Node.js dependencies are installed from the root lockfile: https://vercel.com/docs/functions/runtimes/node-js
- Live production evidence from this firing: deployment `dpl_mx6TeW8f3dw1KTtQrKTKC3DwFEGn` for commit `635c5f3` is READY and promoted, but `/api/mcp` still returns HTTP 500 `FUNCTION_INVOCATION_FAILED` for GET, OPTIONS, and unauthenticated POST. The lazy-loading repair therefore did not remove the runtime failure; the next diagnosis must isolate the handler entrypoint/runtime contract itself rather than guessing at PDF imports.

## CODER II firing — latest release-integrity evidence

- Commit `b73694f` is pushed to both `coder/perfection-foundation-2026-08-21` and `main`.
- Local gates passed after the Web Standard handler change: root TypeScript, app TypeScript, focused MCP handler/transport tests (11 tests), full application regression suite, and production build. The built initial chunk remains approximately 445 KB minified / 142.62 KB gzip.
- Vercel deployment `dpl_9USuXrkMUcYMNYfHu5YKqW36vmsS` for `main` commit `b73694f` is `READY`, `target: production`, and assigned the production alias. The public HTML references `assets/index-Cf9xo7xy.js`; stale `index-K0lrZ2kE.js` is absent.
- The live frontend is healthy at HTTP 200, but `GET /api/mcp`, `OPTIONS /api/mcp`, and unauthenticated `POST /api/mcp` still return HTTP 500 `FUNCTION_INVOCATION_FAILED`. The remaining blocker is a runtime entrypoint/module-load issue, not a stale frontend deployment or local type/test failure.
- Project context: Vercel project `stitch-and-scale-pro-api-server`, team slug `plastic-dudes-projects`, Node.js project setting `24.x`. The documented runtime-log endpoint was queried with the project and team context but did not return a bounded response; the next safe step is to reproduce the deployed handler boundary locally or inspect the deployed lambda artifact using the supported deployment API.
- Source scope for the current feature work remains uncommitted beyond `b73694f`; untracked audit/research files are intentionally excluded from production commits.

- Official Vercel changelog verification: Node.js Vercel Functions support the default Web Standard export `export default { fetch(request: Request) { return new Response(...) } }`; named HTTP method exports remain an alternative. Source: https://vercel.com/changelog/node-js-vercel-functions-now-support-fetch-web-handlers
- Official Functions API reference: Vercel Functions use a Web Handler with standard `Request`/`Response`; the documented examples show named `GET(request: Request)` exports for other frameworks and the reference links to the fetch Web Standard form. Source: https://vercel.com/docs/functions/functions-api-reference
- This rules out the `{ fetch }` shape alone as the remaining cause of the live 500. The next diagnosis must target module initialization or a dependency loaded before `handleMcpRequest` runs.

- Official Vercel runtime configuration documentation states that a non-framework project must add `"type": "module"` to `package.json` or use `.mjs` extensions for JavaScript Functions. The repository root package currently has no `type` field while the Vercel API source uses ESM imports and a Web Standard handler. This is the leading explanation for a successful build followed by `FUNCTION_INVOCATION_FAILED` on every `/api/mcp` request. Source: https://vercel.com/docs/functions/configuring-functions/runtime


## Final deployment verification evidence (2026-08-21)

Official Vercel redeploy reference: https://vercel.com/docs/rest-api/deployments/create-a-new-deployment. It documents `POST /v13/deployments` with an existing `deploymentId` to trigger a fresh build.

Commit `ce23e49` initially failed on Vercel with `lint_or_type_error`: `artifacts/stitch-and-scale/src/lib/brag-card.ts` required explicit `.js` specifiers under NodeNext. Commit `0eabc01` fixed those imports, but Vercel then reported unresolved aliases in `brag-copy.ts`; commit `3edb0d7` replaced those aliases with explicit relative `.js` imports. Vercel then reported the remaining `receipt-lab.ts` alias; commit `ef416ca` replaced it with `./numeric-guard.js`.

Local evidence after `ef416ca`: root TypeScript passed, app TypeScript passed, focused MCP/Brag Card tests passed (5 files, 34 tests), full Vitest passed (175 files, 2,275 tests), and the production Vite build passed.

Vercel deployment `dpl_GG4p7rweRfbLkVsxCz6rsihnSNAP` for `ef416ca` reached READY. A redeploy with `POST /v13/deployments` using that deployment ID returned `dpl_Dd8NFbQputVUoKZCzv4hiuQxQ9cw`, which also reached READY.

Production checks immediately after redeploy: `GET /api/mcp` without an Origin returned 405 as intended. A request carrying the public Origin returned 403 origin-not-allowed for both OPTIONS and POST, indicating the current public origin is not yet reflected in `MCP_ALLOWED_ORIGIN` on the deployed function; this remains an active configuration issue to resolve. The earlier pre-configuration POST without auth returned 503 with the explicit MCP-disabled message, proving the endpoint was then fail-closed when `MCP_API_KEY` was absent.

The project environment API accepted creation of production-scoped `MCP_API_KEY` and `MCP_ALLOWED_ORIGIN` variables with HTTP 201, but the redeployment from an existing deployment still returned 403 for the configured Origin. A fresh Git-source production deployment is required to ensure the current project environment snapshot is loaded rather than relying on an older deployment snapshot.


## MCP registry consistency hardening (2026-08-21)

The local audit found a real contract-quality defect: `getMcpToolNames()` listed `export.brag_card` before `calculate.marketplace_take_rate`, while `getMcpToolDefinitions()` and the live `tools/list` response exposed the reverse order. Membership was identical, but consumers relying on deterministic registry order could observe inconsistent contracts. The definitions were reordered to the canonical sequence and the contract test now asserts that helper names exactly equal the definition names; the transport test was aligned to the same sequence.

Post-fix local evidence: root TypeScript passed, app TypeScript passed, the focused MCP subset passed (4 files, 21 tests), the full Vitest suite passed (175 files, 2,275 tests), and the production build passed. The post-fix source still requires a fresh Vercel deployment before the live order can be rechecked.


## Registry-order release verification (2026-08-21)

Commit `9846ae0` was pushed to both `coder/perfection-foundation-2026-08-21` and `main`. Fresh Git-source production deployment `dpl_EsioRhDUiz2aJtaHXkkjvUyf7D6a` reached READY at `stitch-and-scale-pro-api-server-f2coi3ppf.vercel.app`.

The public alias `https://stitch-and-scale-pro-api-server.vercel.app/api/mcp` now passes the final transport checks: GET returns 405, allowed-origin OPTIONS returns 204 with `Access-Control-Allow-Origin: https://stitch-and-scale-pro-api-server.vercel.app`, authenticated `tools/list` returns HTTP 200 with no JSON-RPC error, and the eight tools appear in canonical order: `project.intake`, `project.validate`, `grading.run`, `grading.explain`, `export.pattern_pdf`, `export.project_book_pdf`, `export.brag_card`, `calculate.marketplace_take_rate`. A request from `https://example.com` returns 403, confirming origin allowlisting remains fail-closed. The deployment URL redirects to the public alias, so the alias is the authoritative live smoke-test target.


## CODER II firing — 2026-08-22: main synchronization and registry-derived public truth

Before committing, the audit branch was reconciled with newer agent work. Local audit head `6aa86f0` was safely fast-forwarded to origin/main commit `5e4cc73` (`CHK-186`, Pattern Compiler and mathematical validation). The truthfulness patch was stashed before synchronization and reapplied; the only merge conflict was the five-locale lab title/ARIA pair in `workspace-copy.ts`. Resolution preserved the compiler release and replaced hardcoded counts with the canonical `TAB_COUNT` expression. The current registry contains 83 entries.

The repair makes landing stats, localized workspace titles and ARIA labels, and onboarding search-description counts derive from `TAB_REGISTRY`/`TAB_COUNT`. Spanish and Portuguese onboarding descriptions use `{count}` interpolation supplied by `TAB_COUNT` at render time. The count-drift, tab-visibility, and revision-snapshot contracts now derive expected totals from the registry instead of encoding historical literals. Source scanning found no remaining 79-lab claims in `src` after the repair.

Commit `53b9cee` was created after staged whitespace validation and pushed to both `coder/perfection-audit-2026-08-22` and `main`. Fresh post-merge evidence: root TypeScript passed; app TypeScript passed; 195 test files and 2,365 tests passed; production build passed in 4.53 seconds. The Vite build continues to emit six non-fatal sourcemap-location warnings for `tooltip`, `dropdown-menu`, `label`, `sheet`, `select`, and `progress`; they do not fail the build but remain a release-hygiene cleanup candidate.

Fresh Git-source production deployment `dpl_2VnNosNsZ8Cn9PsAVWvj5jnAY2i2` was created from full commit `53b9ceea7a440fe25f80b0bc156c29a2371384d6` and reached READY. Public alias checks passed: GET `/api/mcp` returned 405; allowed-origin OPTIONS returned 204; authenticated JSON-RPC `tools/list` returned 200 with all eight tools in canonical order: `project.intake`, `project.validate`, `grading.run`, `grading.explain`, `export.pattern_pdf`, `export.project_book_pdf`, `export.brag_card`, `calculate.marketplace_take_rate`; rejected-origin POST returned 403 with MCP error `-32001`. The deployment-specific URL is protected by Vercel and returned 302/401, so the public alias is the authoritative release check.

The pre-sync stash remains temporarily retained as a recovery precaution and contains only the already-committed pre-sync audit patch; no credentials or `/home/ubuntu/first_novel_invention_brief.md` were modified.


## Exact-main release integrity recheck — 2026-08-22

The repository was rechecked against origin before release: origin/main had advanced from the audit branch’s `6aa86f0` to agent commit `5e4cc73`; the audit branch was fast-forwarded safely, its patch was reapplied, and the resulting correction was committed as `53b9cee`. The evidence-only follow-up commit `d3abf16` was then pushed to both the audit branch and `main`, leaving no uncommitted changes or recovery stashes.

To eliminate any ambiguity between the verified code commit and the current main tip, a fresh Git-source production deployment was created from full commit `d3abf163a29a79a780e4a6f29c4dd00de8315762`: deployment `dpl_9qG2w7A2oSfzwpgHzk5GuPVBPifw` reached READY. The public alias remained healthy after this exact-main deployment: GET `/api/mcp` returned 405, allowed-origin OPTIONS returned 204, authenticated JSON-RPC `tools/list` returned 200 with eight tools in canonical order, and a disallowed origin returned 403 with MCP error `-32001`.


## Final exact-main deployment — 2026-08-22

After the release-evidence commit advanced main to `4213e10189114cfd87aed928764d1f53998bd2b9` (`4213e10`, documentation-only), a final Git-source production deployment was created from that exact main tip: `dpl_6a7o1ZMvp3aX6rUuzxr5ZBMyqCZJ`, which reached READY. The public alias smoke test immediately afterward remained green: GET `/api/mcp` 405, allowed-origin OPTIONS 204, authenticated `tools/list` 200 with eight canonical tools, and rejected-origin POST 403 with error `-32001`. This closes the exact-main release-integrity loop; the deployed application code is the same verified code as `53b9cee`, while the later commits contain only release evidence.

## CODER II firing — 2026-08-22: touch-safe batch selection and provider render integrity
A fresh remote reconciliation found origin/main at `2d09a85`, containing the same substantive `CHK-187`/`CHK-188` agent work as the audit branch but with different commit hashes. A rollback pointer `backup/coder-perfection-audit-before-align-20260822-050941` was created; the audit branch was reset to origin/main and the verified correction was cherry-picked, preserving the newer agent content without duplicate history. The resulting correction was committed as `ed8876e` and pushed to both `coder/perfection-audit-2026-08-22` and `main`.

The integrated batch dashboard had two trust-critical defects. The ProjectsProvider contained an accidental literal `],path:` text node that could render into the application, and project-selection controls were hidden by default in a way that made them unreliable on touch/coarse-pointer devices. The provider marker was removed with a source-integrity regression. Project selectors now remain visible for coarse pointers, retain hover concealment only for fine pointers, expose project-specific stateful `aria-label`/`title` text, use `type="button"`, expose stable test IDs, and stop click propagation before card navigation. A focused dashboard selection suite was added. The staged whitespace warning was removed before the final amend.

Final aligned local gates passed: root TypeScript, app TypeScript, `git diff --check`, 5 focused files/22 focused tests, full Vitest with 196 files and 2,369 tests, and production build in 4.86 seconds. The build output remains code-split; largest listed gzip chunks were `lib-i18n` 55.01 kB, `index` 96.51 kB, and `vendor-ui` 39.87 kB. No provider marker remained in non-test application source.

Fresh Git-source deployment `dpl_35jRGuCLUnaV8k19qKyxNTYcMCEW` was created from full commit `ed8876ef683555aa7ca483b5772cdb6efa2661fd` and reached READY. Public alias checks passed: GET `/api/mcp` returned 405; allowed-origin OPTIONS returned 204; authenticated JSON-RPC `tools/list` returned HTTP 200 with all eight canonical tools and no JSON-RPC error; disallowed-origin POST returned 403 with MCP error `-32001`. The deployment-specific URL redirects/protects access and is not the authoritative smoke target; the public alias remains authoritative. The repository HEAD, origin/main, and the audit branch were synchronized at the verified code commit before this evidence-only note update. No credentials or `/home/ubuntu/first_novel_invention_brief.md` were modified.

## CODER II firing — 2026-08-22: legacy hydration repair, mobile hardening, and release-integrity boundary
The verified publication-integrity correction and legacy-record repair are present on origin/main at merge commit `2e8943828443e32fcd1efb83aad2148a05a44667` (`2e89438`). The included substantive commits are `6615b2c` (invalidate publication approvals after source changes), `609290d` (normalize malformed legacy project records), `82ca81c` (mobile touch-target and smoke coverage), and the reconciled CHK-195 merge. The audit branch and origin/main were checked immediately before this entry; the worktree was clean, and `/home/ubuntu/first_novel_invention_brief.md` remained byte-identical at SHA-256 `5a7668a95841e7e74fc2dcf702cf1ffa94deed06d702911691d0`.

Fresh post-merge local verification passed: root TypeScript, app TypeScript, the complete Vitest suite (202 test files), production build, whitespace validation, protected-file hash validation, and the scripted 320–430px mobile workflow. The mobile workflow covered onboarding, dashboard, new-project validation, workspace, PDF preflight, Grading Lab, and Design Ledger overflow/hit-area checks. The live mobile visit exposed a separate release-integrity problem rather than a local-code failure: the public alias was still serving the older bundle and reproduced `Cannot read properties of undefined (reading 'toLowerCase')` for a malformed persisted project record. The fixed local production build rendered safely with the malformed-record replay, and the normalizer is covered by focused unit and reducer-boundary regressions.

Vercel created READY preview deployment `dpl_7RtjwTQTSh3HiXPWnqxbmgG6G2JL` for exact commit `2e8943828443e32fcd1efb83aad2148a05a44667`. Assigning that deployment to the public alias returned HTTP 200, but it used preview environment configuration: allowed-origin OPTIONS and authenticated tools/list were rejected with 403 / MCP `-32001`, and Vercel’s documented `promote` path returned HTTP 402 `api-deployments-free-per-day`. The public alias was therefore immediately restored to the prior READY production deployment `dpl_66wRKxv2u35twNRKEfiM7tWRC7j2`; this was a deliberate fail-safe rollback, not a claim that the new code is live.

Fresh MCP smoke after rollback passed the trust contract on the safe prior production release: GET `/api/mcp` returned 405 / `-32600`; allowed-origin OPTIONS returned 204; authenticated JSON-RPC `tools/list` returned 200 with exactly the eight canonical tools (`project.intake`, `project.validate`, `grading.run`, `grading.explain`, `export.pattern_pdf`, `export.project_book_pdf`, `export.brag_card`, `calculate.marketplace_take_rate`); and a forbidden origin returned 403 / `-32001`. The current public alias is consequently **safe but stale**: it preserves the prior production MCP environment, while the verified `2e89438` code is not yet promoted to production because Vercel’s deployment quota blocks a production build/promotion.

The public GitHub issues page was also rechecked and reported Open 0 and Closed 69. Remaining release blockers are explicit: (1) the exact `2e89438` code is not live on the public alias; (2) the live alias still reproduces the legacy hydration crash; and (3) fit-governance ease profiles remain declared design intent and are not yet applied to grading math. The next safe release action is to retry production promotion/deployment after the Vercel quota window permits it, then re-run the full alias smoke and the mobile legacy-record replay against the live release. No credentials were written to this log.

## Evidence-tip deployment poll — 2026-08-22
Evidence commit `4ad2a188e769b4e3fe8e6828bab332148ff8abfc` was pushed non-forcibly to both `coder/perfection-audit-2026-08-22` and `main` after origin/main was confirmed at its parent. A six-attempt Vercel inventory poll found no deployment for the exact `4ad2a18` tip. This documentation-only commit does not alter runtime behavior; the public alias remains intentionally assigned to the prior production deployment `dpl_66wRKxv2u35twNRKEfiM7tWRC7j2`, whose MCP smoke remains green. The exact verified application tip still requires a future production build/promotion after the Vercel daily deployment quota resets.

## CODER II firing — 2026-08-22: fail-closed artifact inspection quality
The verified CHK-196 artifact-inspection hardening is committed on origin/main at `a1bbec0e65c015afed2536b5f2e8457a361588c1` (`a1bbec0`). The reducer now normalizes inspection reports at the persistence boundary: explicit checklist failures force a failed report and failed quality snapshot; incomplete or unknown evidence remains warning/pending; a green snapshot requires positive page count plus explicit clean blank-page, title, headings, and table-continuity evidence. The Artifact Inspection UI applies the same downgrade behavior before saving. Only four source/test files were included; no generated assets, credentials, or protected invention files were changed.

Fresh local verification passed after the final patch: root TypeScript, app TypeScript, the complete Vitest suite with 205 test files and 2,406 tests, production build, whitespace validation, and protected-file hash validation. The production build still emits six pre-existing Vite sourcemap-location warnings for shared UI primitives but exits successfully. The protected invention brief remains byte-identical at SHA-256 `5a7668a95841e7e74fc2dcf702cf1ffa94deed06d7029116919dcc0489b609ce`.

The commit was pushed non-forcibly to both the audit branch and `main`; remote refs were confirmed at `a1bbec0`. Vercel inventory polling found no deployment for the exact tip, and a documented production deployment request returned HTTP 402 `payment_required` / `api-deployments-free-per-day` because the daily deployment resource limit is exhausted. The public alias therefore remains intentionally on the prior safe production deployment and does not yet contain this correction.

Fresh public MCP smoke on the restored alias remains green: GET `/api/mcp` returned 405; allowed-origin OPTIONS returned 204; authenticated `tools/list` returned 200 with exactly the eight canonical tools; and forbidden-origin POST returned 403 with MCP error `-32001`. A fresh mobile-style browser visit still fails before workspace render on malformed legacy state, displaying the localized route-error screen with technical detail `Invalid time value`; this confirms the alias is stale, not that the verified local fix failed. The public GitHub API currently reports two open records, both pull requests: #71 `feat(domain): knitwear Pattern QA and publication preflight` and #70 `feat(ui): implement dual-tier mobile navigation tabs and update storage copy`. Remaining release blockers are the stale public alias/Vercel quota, live legacy-state crash, and non-operational ease-profile math; the six sourcemap warnings and oversized public assets remain quality risks rather than immediate gate failures.

## CODER II firing — 2026-08-22: invalid legacy timestamp normalization

The highest-impact defect found after the PWA hardening was a second legacy-record crash on the public application: malformed persisted timestamps reached dashboard date sorting/relative-time formatting and produced `Invalid time value`. The prior normalizer repaired missing timestamps but accepted invalid non-empty strings. The correction in `e4755364e381b8f4bf4b2760d99c0223eb9a2b57` validates `createdAt` and `updatedAt` with `Date.parse`, repairs invalid values to a deterministic safe timestamp, and safely handles an invalid migration `now` input. Focused regression coverage proves invalid timestamps are repaired while valid project data remains preserved.

Fresh local verification passed before commit: root TypeScript, app TypeScript, the complete Vitest suite with 205 test files and 2,407 tests, production build, whitespace validation, protected-file hash validation, and the scripted 320–430px mobile workflow covering onboarding, dashboard, new project, sample workspace, export preflight, Grading Lab, and Design Ledger. The production build still emits six pre-existing Vite sourcemap-location warnings but exits successfully. The protected invention brief remains byte-identical at SHA-256 `5a7668a95841e7e74fc2dcf702cf1ffa94deed06d7029116919dcc0489b609ce`.

The code commit `e475536` was pushed non-forcibly to both the audit branch and `main`, with remote refs confirmed equal. A documented exact-commit production deployment request for `e475536` returned HTTP 402 `payment_required` / `api-deployments-free-per-day`; the existing known production deployment `dpl_HBKJ1FFaCr5TyeZemg5Tx8Bv3Kva` remains READY for prior commit `b90042f`, while the public alias therefore still serves the pre-timestamp-fix bundle. Fresh public MCP smoke remains green on the safe alias: GET `/api/mcp` 405 / `-32600`, allowed-origin OPTIONS 204, authenticated `tools/list` 200 with exactly the eight canonical tools, and forbidden-origin POST 403 / `-32001`. The live browser still shows the localized `Invalid time value` route error because the new fix cannot be live until Vercel permits deployment. This is a release-integrity blocker, not a failed local regression.

The current public GitHub issue signal remains two open pull requests (#70 and #71) and no open issues. Remaining blockers are the stale public alias/Vercel quota, live malformed-state crash until promotion, and ease profiles that remain declared intent rather than active grading math. No credentials or protected invention files were written to this log.


## CODER II firing — 2026-08-22: exact-main production proof and artifact-test hygiene

The audit branch was fetched and reconciled before the bounded correction; both local `HEAD` and `origin/main` were at `f88f6a4f2ff07355f57b192929a79e508fe6a2ae` (`[CHK-197] [STITCH-AND-SCALE-PRO] [VERIFIED] Technical-editor defect ledger`). The protected invention checkpoint `/home/ubuntu/first_novel_invention_brief.md` still hashes to `5a7668a95841e7e74fc2dcf702cf1ffa94deed06d7029116919dcc0489b609ce`.

The artifact-inspection reducer test harness was isolated from browser-only IndexedDB persistence with a test-only `vi.mock('@/lib/storage-lib', ...)`. This does not change production persistence; it prevents pure reducer tests from emitting expected `indexedDB is not defined` noise. The focused suite passed 7/7 tests and app typecheck passed after the correction.

Fresh full gates after the correction passed: root TypeScript, app TypeScript, `git diff --check`, full Vitest (207 files / 2,413 tests), production build, and the scripted mobile workflow at 320/360/390/430px covering onboarding, dashboard, new project validation, sample workspace, export preflight, Grading Lab, and Design Ledger. The build completed in approximately 8.53 seconds. The build still emits six known non-fatal sourcemap-location warnings for `tooltip.tsx`, `label.tsx`, `dropdown-menu.tsx`, `sheet.tsx`, `select.tsx`, and `progress.tsx`; the build is passing, not warning-free.

Vercel deployment `dpl_3MJzbHTi1p1s4Wtfobbmpo8FGjiK` is `READY`, `target: production`, `readySubstate: PROMOTED`, and its Git metadata is the exact main SHA `f88f6a4f2ff07355f57b192929a79e508fe6a2ae`. Project metadata assigns `stitch-and-scale-pro-api-server.vercel.app` and its branch aliases to that deployment. The public alias therefore now serves the exact f88f6a4 release rather than the previous b90042 deployment.

Fresh public MCP boundary evidence after that promotion: `GET /api/mcp` returned HTTP 405 with JSON-RPC `-32600`; allowed-origin `OPTIONS` returned 204; authenticated `tools/list` returned HTTP 200; rejected-origin POST returned HTTP 403 with JSON-RPC `-32001`. The authenticated registry response contains the canonical eight-tool order: `project.intake`, `project.validate`, `grading.run`, `grading.explain`, `export.pattern_pdf`, `export.project_book_pdf`, `export.brag_card`, `calculate.marketplace_take_rate`.

A controlled sandbox-browser replay wrote one malformed project with missing name/author/gauge/sections and invalid timestamps to both localStorage and IndexedDB, after saving the original values in sessionStorage. The exact public alias reloaded successfully in Spanish and rendered a normalized `Untitled pattern` draft with valid defaults and a relative timestamp; neither `Invalid time value` nor the previous `toLowerCase` crash appeared. The original 58-project local-first state was then restored and confirmed after reload. This is browser evidence for the e475 timestamp-normalization fix on the now-promoted f88 release.

Residual release risks remain: live PDF/Project Book/Brag Card/receipt visual and downloaded-artifact evidence still needs a dedicated fresh pass; public logo/app-icon images remain oversized; six sourcemap warnings remain; the ease profile is declared intent and is not grading math; and no readiness claim should be made until the exportable artifact and PWA update workflows have fresh live evidence. The Vercel and MCP credential files were used without printing their values; rotate/revoke GitHub and Vercel credentials after handoff.


## CODER II firing — 2026-08-22: SPA deep-link repair, concurrent CHK-198 reconciliation, and quota blocker

Origin/main advanced concurrently from `4ef283d` to `7375a17` (`CHK-198`, test-knit rounds and archival records). The audit branch was safely fast-forwarded before applying the bounded routing correction; all CHK-198 source, test, and documentation paths were preserved. The corrected commit `8eeeab6237fc93498ebbb17bd3198995046ec751` (`8eeeab6`) was then pushed non-forcibly to both `coder/perfection-audit-2026-08-22` and `main`, with both remote refs confirmed equal.

The fresh local production-build gates on the reconciled `7375a17` source plus the routing correction passed: root TypeScript, app TypeScript, full Vitest with 208 files and 2,415 tests, production build in 9.44 seconds, `git diff --check`, protected-file hash, and the scripted mobile workflow at 320/360/390/430px. The mobile workflow covered onboarding, dashboard, new project, sample workspace, export preflight, Grading Lab, and Design Ledger. The build remains passing but not warning-free: six known non-fatal sourcemap-location warnings remain for `tooltip.tsx`, `label.tsx`, `dropdown-menu.tsx`, `sheet.tsx`, `select.tsx`, and `progress.tsx`. The largest current public images remain `logo.png` 2,892,369 bytes, `app-icon.png` 2,892,369 bytes, `favicon.png` 1,135,233 bytes, and `og-image.png`/`app-logo.png` 610,962 bytes each. The protected invention brief remains byte-identical at SHA-256 `5a7668a95841e7e74fc2dcf702cf1ffa94deed06d7029116919dcc0489b609ce`.

The live release inventory shows the audit-branch preview for `8eeeab6` is still BUILDING and has no production target. The exact Git-source target=production deployment request for `8eeeab6` returned HTTP 402 `payment_required` with Vercel code `api-deployments-free-per-day` and message “Resource is limited - try again in 24 hours (more than 100...)”. The current authoritative public production deployment remains `dpl_3MJzbHTi1p1s4Wtfobbmpo8FGjiK`, READY/PROMOTED, target production, for older main SHA `f88f6a4f2ff07355f57b192929a79e508fe6a2ae`. The public root returns HTTP 200, but the sample-project deep link returns Vercel `404: NOT_FOUND`; the new named-parameter SPA fallback is therefore not live. This is a release-integrity blocker, not a local routing-test failure.

The public MCP boundary remains green on the safe older production deployment: GET `/api/mcp` returns 405 / JSON-RPC `-32600`; allowed-origin OPTIONS returns 204 with the public alias; authenticated `tools/list` returns 200 with exactly the canonical eight tools in order (`project.intake`, `project.validate`, `grading.run`, `grading.explain`, `export.pattern_pdf`, `export.project_book_pdf`, `export.brag_card`, `calculate.marketplace_take_rate`); and forbidden-origin POST returns 403 / `-32001`. GitHub’s public API reports two open records, both pull requests: #70 and #71; no ordinary open issue was found.

A fresh local browser checkpoint rendered the sample workspace and Pattern PDF export preflight. The PDF route showed four templates, logo upload, three include switches, custom filename `sample-crew-neck-2026-proof`, `Ready to print`, `0 errors · 0 warnings`, and a six-page live preview. The Export PDF trigger produced no browser-console error but uses the browser print dialog, so a binary saved-PDF artifact was not captured by automation. Fresh Project Book, Brag Card, and receipt export visual/download evidence remains outstanding. The routing research and browser checkpoint are recorded in `docs/verification/vercel-spa-routing-research-2026-08-22.md`, `docs/verification/live-browser-checkpoint-2026-08-22.md`, and `docs/verification/mobile-smoke-2026-08-22.md`.

Residual blockers and risks are explicit: (1) exact `8eeeab6` is not live because Vercel quota blocks production deployment; (2) public deep links still 404; (3) live exportable artifact evidence is incomplete; (4) public images are oversized; (5) six sourcemap warnings remain; and (6) ease profiles remain declared intent, not grading math. No credentials or protected invention files were written to the log. Rotate/revoke GitHub and Vercel credentials after handoff.


## Exact-main inventory follow-up — 2026-08-22

A subsequent Vercel inventory poll shows the audit-branch deployment for `8eeeab6` transitioned from BUILDING to `READY` with `readySubstate: STAGED`, still with no production target. This is a preview/staged deployment and was not assigned to the public alias because its environment is not the verified production snapshot. No deployment for the newer evidence-only main tip `4af5fd3` appeared. The exact target=production request remains blocked by HTTP 402 `payment_required` / `api-deployments-free-per-day`; the public alias remains on `dpl_3MJzbHTi1p1s4Wtfobbmpo8FGjiK` for older SHA `f88f6a4`.
