# Cycle log — 2026-08-21 — CHK-148

## Directive
Continued-queue protocol, no-queueing mode (owner message 2026-08-21: "I hope you working on everything and not queuing them"). Working the first queued item after CHK-147: **QUEUE-017-GATE** (CRITICAL) — the workspace status chip shows only save status, with no project-wide **data-validity** flag (extended-audit E-06-adjacent finding; status chip without data-validity flag).

## What changed

| File | Change |
| --- | --- |
| `src/lib/project-validity.ts` (new) | Pure analyzer `analyzeProjectValidity(project)`: walks sections → measurements (missing/zero/out-of-bounds base values, invalid types), gauge completeness, and pattern-dimension sanity. Returns `{valid, invalidItems[{section, measurement, reason, fix}]}`. |
| `src/lib/project-validity.test.ts` (new) | 8 regression tests: valid project; each violation class; cap on reported items (never more than 3 distinct, first three by severity). |
| `src/lib/grading-lab.ts` | Exported `invalidMeasurements` for reuse by the analyzer. |
| `src/components/autosave-indicator.tsx` | New `dataValidity` prop: invalid → amber chip "Saved — invalid data" (FR: "Enregistré — données invalides") with a tooltip listing the invalid measurements and their fixes. |
| `src/components/shell.tsx` | Computes the validity report for the active project via `useProjects()` and passes it to the indicator. |
| `src/lib/lab-stat-copy.ts` | Keys `statusSavedInvalid`, `invalidDataTooltipTitle` in 5 locales. |

## Gates
`tsc` clean; `vitest` **2,130/2,130 passing (149 files)**; production build green (6.6s). `[VERIFIED]` justified.

## Evidence (live, FR session)
`docs/evidence/chk148-invalid-data-chip-fr.webp` — header shows the amber chip "Local uniquement · Saved — invalid data" after seeding a zero-value measurement. Post-restore reload shows the chip absent (valid state verified programmatically and visually).

## Notes
The chip appears only during the ~2 s save-status window after a save completes — same behavior as the existing status chip. The tooltip survives hover during that window. This is consistent with the existing AutosaveIndicator design contract.

## Ledger
QUEUE-017-GATE → done, CHK-148. Next queue item: QUEUE-005 (legend overcount eyes-check; programmatic closure already verified by Reviewer — eyes-pass only).
