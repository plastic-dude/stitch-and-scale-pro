# Cycle 2026-08-17 — CHK-125

## Lead
Manus (team lead), working the existing defect backlog only. Cycle fired on
user report, not an inbox item — user reports enter the backlog as defects
like any other Reviewer/QA finding.

## Report (CHK-125) — `efaf7a9`
User: "I also can't find the multiple different tabs we used to have."

**Verdict of diagnosis: no tabs were lost.** All 79 workspace tabs remained
present and functional on the current tree. The defect was a discoverability
regression: the 6-chip group row introduced in CHK-120 as the mobile/tablet
substitute for the flat strip carried no `lg:hidden` guard and rendered at
all widths, sitting directly above the flat 79-tab strip on desktop. Six
group chips read like "the tabs", so the strip looked gone.

**Fix (minimal):**
1. `src/pages/project-workspace.tsx` — chip row now `lg:hidden`; visible only
   below 1024px where the strip is genuinely hidden.
2. Chip counts now derive from `TAB_REGISTRY` (the strip's single source of
   truth) instead of the `TAB_GROUPS` frequency count — removes a latent
   coupling that could place a stale number on a chip. Verified identical
   today (design 12, fit 7, pricing 15, launch 13, channels 10, business 22).

**Regression tests** (`src/tab-visibility.test.ts`, 5 structural/fs-based,
project convention): chip row `lg:hidden`; chips visible below lg; flat strip
`hidden lg:flex`; grouped navigator `lg:hidden`; chip count = registry filter
with pinned per-group counts.

**Gates (measured on current tree):** typecheck clean · vitest 1844/1844
(+5 new) · production build green · localhost visual verification: 6-chip row
absent on desktop, full strip present and selectable.

## Backlog state
All GitHub issues closed (#13 closed mechanistically in CHK-124 with real
printed-output evidence). No open PRs. No fresher QA branches than the
2026-08-17 auditor cycle (QA-59, worked in CHK-123/CHK-124). Backlog is
genuinely empty — next CHK comes from Reviewer/QA input, a user report, or a
justified surface audit per standing rules.

## Honest residuals
1. The printed grading sheet still carries hardcoded English UI strings
   ("MEASUREMENT" header, "Drafted by…") — localization-gap candidate on the
   print surface, parked in the #13 closing comment; next localization fix
   if requested.
2. Headless Chromium `--print-to-pdf` does not evaluate `@media print` color
   rules — documented in the CHK-124 notes; print-media emulation
   (Puppeteer CDP) is the verified substitute, reusable script preserved.
3. Rollup chunk-size warnings persist in the build (pre-existing, bundle
   growth logged by Claude in an earlier audit) — not a defect, known cost.

## Instructions for agents
**REVIEWER:** Next review should re-verify that (a) the group-chip row never
renders at lg+ widths (a regression of this exact defect would be severe —
it hides the primary navigation again), and (b) any new tab added to
`tab-registry.ts` updates all three synchronized structures: registry,
`workspace-tab-groups.ts`, and the localized label mapping in
`workspace-tab-labels.ts`. Flag any fourth copy of this classification.
Also re-check the parked print-surface localization residual.

**CRAWLER:** Visual crawl at three viewports (<768px, 768–1023px, 1024px+):
below 1024px the 6 group chips + grouped navigator (sheet/dropdown) must be
the only tab navigation; at 1024px+ only the flat strip + header "All Labs"
dropdown. Click at least one chip and one strip tab at each viewport to
confirm the selected panel swaps. Re-crawl the print surfaces (Grading Print
Sheet, Export PDF, Receipt Lab print) — the CHK-124 black-on-white print
fix must still hold. Report any tab that fails to swap content.

## Ledger
CHK-125 — tab discoverability, committed `efaf7a9`, notes `docs/leader-notes/`.
