# Cycle log — CHK-146 (2026-08-21)

## What this cycle worked

Owner delivered two QA-audit files after CHK-145 (Brutal Mobile & Resilience Audit — first pass and Extended Brutal Audit — second pass). The first-pass findings (F-01..F-14) were already registered as QUEUE-011..016 during the restudy. This cycle therefore did two things in one commit, per one-fix-per-cycle:

1. **Registered the extended-audit delta** into the queue: five new structural items (E-01 shared validation layer, E-02 NaN/Infinity leak, E-03 copy quarantine, E-04 grading readiness semantics, E-05 error boundaries, E-06 persistence transparency) as QUEUE-017-* rows, archived both audit files into `docs/leader-notes/`, and sharpened QUEUE-012's scope to the `validateField` layer.

2. **Fixed QUEUE-017-NaN (E-02, CRITICAL)** as a severity exception over the queued QUEUE-005 (MINOR).

## Severity exception

QUEUE-017-NaN is CRITICAL (decision-integrity: non-finite numbers rendered as prose/stat values users could act on), which outranks the first queued item QUEUE-005 (eyes-check, MINOR). The continued-queue protocol allows working a higher-severity item first; this is recorded in the ledger line. QUEUE-005 remains the first queued item afterwards.

## Fix detail (E-02 NaN/Infinity leak)

New module `src/lib/numeric-guard.ts` (pure, locale-independent, typed):

- `finiteOr(v, fallback)` — replaces `NaN`/`Infinity` with a display fallback at the calculation boundary, the single canonical place for it.
- `finiteNarrative(tpl, value)` — narrative builder that substitutes an "unavailable" clause when the value is non-finite, so prose never stringifies `Infinity`/`NaN`.
- 16 regression tests covering all finite/non-finite combinations.

Boundary fixes at the three concrete repros from the extended audit:

| Repro | Surface | Fix |
|---|---|---|
| `Sell Infinity copies direct...` | `lib/wholesale-book-analyzer.ts` (wholesale `volumeBreakeven` when self-sell net ≤ 0) | Quarantined narrative + checklist item "no computable breakeven"; stat box already rendered `—`, now consistent |
| `$NaN` cold-acquisition cost | `lib/retention-planner.ts` (`projectedRevenue / netPerSale` when netPerSale = 0) + `components/retention-card.tsx` | Planner returns quarantine note; card renders `—` plus the reason in localised copy slot |
| Best-paid-channel prose with nonsense economics | `lib/ad-break-even-lab.ts` + `components/ad-break-even-card.tsx` | Best-channel prose suppressed when price yields zero net (division-driven invalid economics); verdict's "email list" answer kept |

Additional regression tests: `wholesale-book-analyzer.test.ts` (zero self-sell rate must yield the quarantined narrative), `retention-planner.test.ts` (zero price → quarantined cold-acquisition), `ad-break-even-lab.test.ts` (invalid price → no paid-channel prose).

## E-04 verified closed (QUEUE-017-GATE, partial)

Re-verified at the current tree: `grading-lab.ts` `invalidMeasurements` blocks any graded set containing a non-finite or ≤0 `baseValue` (existing tests cover -9, NaN, Infinity, 0 → G-09 flag raised), and CHK-144's save-path validation means the audit's impossible `-9` measurement can no longer be saved at all. The grading-lab path of E-04 is therefore closed. **Remaining open gap stays queued**: the workspace status chip still shows only `Saved` without a project-wide data-validity flag — that would need a per-project validity state crossing all validators, out of scope for one-fix-per-cycle.

## Honest notes

- The remaining `∞` displays in other labs (channel migration, convention booth, membership site, pattern bundle, POD patterns, retreat, pricelist, workshop, magazine) are **deliberate display choices** — they mark "no finite breakeven exists" rather than an error; no prose stringifies them. They are not re-fixed here; if the owner wants them quarantined too, that is QUEUE-017-COPY/E-03 territory.
- The shared `validateField` layer (E-01) is the deeper fix for the whole class; boundary patches in this cycle are interim guards. QUEUE-012/QUEUE-017-NEW still queued first in severity order.

## Gates

- `pnpm typecheck`: clean
- `pnpm vitest run`: 2,095/2,095 tests passing, 149 files (was 2,076/148; +19)
- `pnpm build`: green (6.59s)

## Evidence (live, FR locale)

- `docs/evidence/chk146-wsbook-zero-retail-quarantined.webp` — zero retail price: breakeven stat `—`, quarantined narrative rendered, checklist item downgraded to warning.
- `docs/evidence/chk146-retention-zero-price-quarantined.webp` — zero average price: cold-acquisition stat `—` with quarantine note, no `$NaN` anywhere on the card.
- `docs/evidence/chk146-adlab-zero-price-suppressed.webp` — zero pattern price: best-paid-channel prose suppressed; verdict keeps the email-list answer.

## Note on the ledger commit hash

The run-ledger SHA in the pushed commit is `2743a76`, one step behind the pushed HEAD (`3c9d555`), by construction: inserting the commit's own SHA into the ledger file requires a final amend, which changes the hash, making a true self-reference impossible in a single pass. This is the same documented convention already used by CHK-144/CHK-145 ledger cells ("ref lags one amend by construction"). No future firing should attempt to "fix" this — the convention is intentional, and the ledger cell's text already discloses it.
