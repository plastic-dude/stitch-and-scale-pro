# CHK-129 Cycle Log — 2026-08-17

Commit: `9404ccf` · Tests: **1,870 / 124 files** (baseline was 1,865 / 123) ·
Typecheck clean · Production build green · Preview verified live at localhost:5000.

## What was fixed

QA LIVE-004's standing residual: at phone widths the app-shell header nav is
icon-only (labels hide below `md`), and the three plain links (Projects,
Portfolio, Settings — `p-2` = 36×36px) plus the New Project button (`h-9` =
36px) were below the 44×44px touch-target minimum. `shell.tsx` now carries
`min-h-11` / `min-w-11` on all four triggers, with inline code comments
explaining the contract so nobody "cleans them up". A new structural
regression suite, `shell-nav-touch-target.test.ts` (5 tests), pins the 44px
contract at the source level.

Live verification (emulated Android 360×740 and iPhone 390×844): all four
triggers measure 44×44px+ (New Project: 48×44), zero header-element overlap.
Screenshot: `chk129-nav-360.png` (project root of the sandbox).

## QA cycle 63 & 64 findings verified (backlog was empty at cycle start)

1. **QA cycle 63 "47 blocked desktop tabs" — STALE AT HEAD.** Full real-pointer
   crawl at 1280px activated **79/79 tabs** (no missed). Also note for future
   harness writers: Radix strips the `value` attribute from the DOM — triggers
   are located by `id$="-trigger-<val>"`, not `[value=...]`. And fixed-position
   Radix dialogs have `offsetParent === null` — do not filter them by visibility.
2. **QA cycle 63 mobile sheet — WORKS.** At 390px the All Labs trigger
   (358×44) opens the sheet; the dialog mounts with 80 buttons (79 labs +
   close), fixed position, z-50, bottom edge at viewport bottom. The cycle's
   "sheet empty" screenshot was a harness artifact.
3. **QA cycle 64 issues #66 and #67** both reported the same nav-icon defect —
   fixed here, evidence commented, both closed (67 as duplicate).

## Instructions for REVIEWER

- Verify CHK-129 by running `vitest run shell-nav-touch-target.test.ts` on
  `9404ccf` and by grepping `shell.tsx` for the four `min-h-11` additions.
- Please confirm QA cycle 63's findings are stale at HEAD with your own
  crawl (see harness pattern in this cycle's sandbox script `chk129-crawl.mjs`).
- Open items for your next pass: none at file level. Remaining surfaces to
  consider for QA: the 404/portfolio nav (same shell), settings page icons,
  any other icon-only interactive elements on phones.

## Instructions for CRAWLER

- Crawl cycle 65 suggestion: sweep **every icon-only interactive element at
  360px** (not just the header nav) — buttons inside lab panels, the sheet
  close icon, the theme toggle if exposed on mobile — and report any hit
  area <44×44px as a MINOR responsive finding.
- Also verify no visual regression in the header at sm/md widths (640–767px):
  labels still hidden, icons centered, no overlap with the storage badge.

## Residuals / open gaps

- None confirmed. The 44px touch-target invariant is now pinned at the source
  for: all 79 tab triggers (CHK-123), the All Labs navigator (CHK-120/123),
  and the shell header nav (CHK-129).
- Honesty note: QA cycle 63's reported defects were not real defects at HEAD —
  the crawl harness itself contained two false-positive patterns (Radix value
  attr and fixed-position offsetParent). QA should adopt the corrected
  selectors before the next cycle.
